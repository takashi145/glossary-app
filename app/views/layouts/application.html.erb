<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- navbar -->
    <div class="navbar relative top-0 bg-base-300 shadow z-50">
      <div class="flex-1">
        <%= link_to "Term List", categories_path, class: "p-3 font-bold normal-case text-xl" %>
      </div>
      <div class="flex-none">
        <ul class="flex items-center space-x-4 px-1">
          <li>
            <%= link_to "カテゴリ集", categories_path, class: "hover:underline #{ "underline cursor-default" if controller_name == 'categories' && action_name == 'index' }" %>
          </li>
          <li>
            <%= link_to "用語集", terms_path, class: "hover:underline #{ "underline cursor-default" if controller_name == 'terms' && action_name == 'index' }"  %>
          </li>
          <li>
            <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn' %>
          </li>
        </ul>
      </div>
    </div>
    <!---->

    <% if notice || alert %>
      <p class="notice"><%= notice %></p> 
      <p class="alert"><%= alert %></p>
    <% end %>

    <div class="">
      <%= yield %>
    </div>
  </body>
</html>
